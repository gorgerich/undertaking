# ⚡ БЫСТРЫЙ СТАРТ - Перенос на GitHub

## 🎯 За 5 минут

### 1️⃣ Создайте проект локально
```bash
mkdir tihaya-pamyat-funeral-site
cd tihaya-pamyat-funeral-site
mkdir -p src/components/ui src/components/figma src/styles public
```

### 2️⃣ Скопируйте ВСЕ файлы

**60 файлов из Figma Make → в новый проект:**

| Откуда | Куда |
|--------|------|
| `/App.tsx` | `src/App.tsx` |
| `/styles/globals.css` | `src/styles/globals.css` |
| `/components/*.tsx` | `src/components/*.tsx` (18 файлов) |
| `/components/*.ts` | `src/components/*.ts` (2 файла) |
| `/components/ui/*.tsx` | `src/components/ui/*.tsx` (35 файлов) |
| `/components/ui/*.ts` | `src/components/ui/*.ts` (2 файла) |
| `/components/figma/*.tsx` | `src/components/figma/*.tsx` (1 файл) |

### 3️⃣ Создайте конфиги

**Скопируйте содержимое из этих файлов:**

1. `PACKAGE.json` → `package.json`
2. `VITE.config.ts` → `vite.config.ts`
3. `TSCONFIG.json` → `tsconfig.json`
4. `INDEX.html` → `index.html`
5. `MAIN.tsx` → `src/main.tsx`
6. `GITIGNORE.txt` → `.gitignore`
7. `POSTCSS.config.js` → `postcss.config.js`
8. `README_FULL.md` → `README.md`

### 4️⃣ Git и GitHub
```bash
# Инициализация
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на github.com
# Затем:
git branch -M main
git remote add origin https://github.com/USERNAME/tihaya-pamyat-funeral-site.git
git push -u origin main
```

### 5️⃣ Установка и запуск
```bash
npm install
npm run dev
```

Откроется на http://localhost:3000 🎉

### 6️⃣ Деплой на Vercel
1. Зайдите на vercel.com
2. Import GitHub repository
3. Deploy
4. Готово! ✨

---

## 📋 Быстрая проверка

Убедитесь что все файлы на месте:

```bash
# Должно показать ~60 файлов
find src -name "*.tsx" -o -name "*.ts" -o -name "*.css" | wc -l

# Структура должна быть такой:
src/
├── App.tsx
├── main.tsx
├── components/     (20 файлов)
│   ├── ui/         (37 файлов)
│   └── figma/      (1 файл)
└── styles/         (1 файл)
```

---

## 🆘 Проблемы?

**Не запускается?**
```bash
node --version  # Нужен 18+
rm -rf node_modules package-lock.json
npm install
```

**Импорты не работают?**
- Проверьте что в App.tsx импорты начинаются с `./`
- Пример: `import { HeroSection } from "./components/HeroSection";`

**Tailwind не работает?**
- Проверьте что `postcss.config.js` создан
- Перезапустите `npm run dev`

---

## ✅ Всё работает?

Теперь у вас:
- ✅ Код на GitHub
- ✅ Локальная версия работает
- ✅ Можно деплоить на Vercel

**Успехов! 🚀**
